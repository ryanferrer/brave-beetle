let champsOBJ = require('./champions.json');
const fs = require('fs');

const trimChampsOBJ = (object) => {
  let newChampsList = {};

  
  for (key in object) {
    let newChampDescription
  }
}

trimChampsOBJ(champsOBJ);