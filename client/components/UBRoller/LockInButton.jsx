import React from 'react';

const LockInButton = () => {
  return (
    <div id="lock-in-button">
      <button type="button" className="btn btn-primary">Lock In!</button>
    </div>
  );
}

export default LockInButton;